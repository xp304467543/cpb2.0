<?xml version="1.0" encoding="utf-8"?>
<com.fenghuang.baselib.widget.round.RoundFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:rv_cornerRadius="@dimen/dp_10"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:foreground="?attr/selectableItemBackgroundBorderless">

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:placeholderImage="@mipmap/ic_placeholder"
        app:placeholderImageScaleType="fitXY"
        app:roundedCornerRadius="@dimen/dp_10"
        app:actualImageScaleType="focusCrop"
        tools:ignore="ContentDescription" />


    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="10dp"
        android:textColor="@android:color/white"
        android:textSize="14sp"
        tools:text="我是标题" />

</com.fenghuang.baselib.widget.round.RoundFrameLayout>